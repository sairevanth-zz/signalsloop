export default function AuthCallbackTest() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-green-600 mb-4">✅ Auth Callback Route Works!</h1>
        <p className="text-gray-600">This page is accessible, so the auth callback route should work too.</p>
      </div>
    </div>
  );
}
